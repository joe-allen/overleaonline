/**
 * @file <%= nameSpaced %>
 */

/**
 * <%= nameSpaced %> component
 */
const v<%= nameUpper %> = () => {
	const <%= nameLower %> = document.querySelector( '.v-<%= name %>' );

	if ( ! <%= nameLower %> ) {
		return;
	}
};

export default v<%= nameUpper %>;

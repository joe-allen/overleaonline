{% include "hero/hero.twig" with {
	class: 'v-hero--sm'
} %}

{% include "recent-news/recent-news.twig" with {
	title:    'What\'s Happening',
	items: posts,
	subtitle: 'News around the neighborhood',
	cta: 'View More'
} %}

{% include "pagination/pagination.twig" with {
	pagination: posts.pagination
} %}

{# {% if post.meta('ic') %}
	{% include "image-content/image-content.twig" with {
		class: 'v-image-content--reverse',
		items: post.meta('ic'),
	} %}
{% endif %} #}

{% include "cta/cta.twig" with {
	title: options,
	cta: options,
	class: 'v-cta--bottom-overlap',
	type: 'Newsletter',
} %}

{# {% include "sponsors/sponsors.twig" with {
	title:    'Our Sponsors',
	count: 6,
	subtitle: 'Sponsor images should be 200x200px only. Can we encourage sponsors to use their logo / icons instead of words? <a class="v-link" href="#!">View All</a>'
} %} #}

{% include "form/form.twig" with {
	title:    'Contact Us',
	subtitle: 'We\'re all ears!'
} %}


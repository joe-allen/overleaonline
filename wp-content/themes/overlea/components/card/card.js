/**
 * @file Card
 */
import { gsap } from 'gsap';

/**
 * Card component
 */
const vCard = () => {
	const card = document.querySelector( '.v-card' );

	if ( ! card ) {
		return;
	}
};

export default vCard;
